package services

// objectives
// user has to log into the application/ create account with email and password or google account
//

type Event struct {
}

type TimeSlot struct {
}

// step 1 convert calendar data to events
func convertCalendarDataToEvents() []Event {
	return []Event{}
}

// step 2 convert events to a list of time slots
func convertEventsToTimeSlots(events []Event) []TimeSlot {
	return []TimeSlot{}
}

// step 3 find the available time slots
func findAvailableTimeSlots(timeSlots []TimeSlot) []TimeSlot {
	return []TimeSlot{}
}
